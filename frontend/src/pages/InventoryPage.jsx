import React from 'react';
import { Typography } from '@mui/material';

function InventoryPage() {
  return <Typography variant="h4">Inventory Page (CRUD UI similar to Customers - TODO)</Typography>;
}

export default InventoryPage;